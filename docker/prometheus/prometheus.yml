global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: atlas-quant
    static_configs:
      - targets:
          - host.docker.internal:9090   # métricas Python del atlas_quant_core
    metrics_path: /metrics
    scrape_timeout: 4s
