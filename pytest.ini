[pytest]
addopts = -ra
norecursedirs =
    .git
    .venv
    venv
    .temp_venv
    __pycache__
    snapshots
    logs
    temp_*
    _generated
    pip
markers =
    unit: fast isolated unit tests
    integration: integration tests with mocked collaborators
