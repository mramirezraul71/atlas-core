# Atlas Code-Quant — Dependencias
# Verificado contra Python 3.14 (sistema)
# Instaladas (no necesitan instalarse):
#   pandas, numpy, fastapi, uvicorn, ccxt, yfinance,
#   aiohttp, redis, celery, APScheduler, websockets,
#   pydantic, plotly, matplotlib, httpx, requests

# A instalar:
sqlalchemy>=2.0.0
scikit-learn>=1.4.0
scipy>=1.12.0
statsmodels>=0.14.0
ta>=0.11.0
backtrader>=1.9.78
xgboost>=2.0.0
lightgbm>=4.3.0
joblib>=1.3.0

# ── RL / AI autónomo (Grok/xAI criterio 2026) ────────────────────────────────
# Motor de Reinforcement Learning — PPO, DQN, A2C
# Instalación: pip install stable-baselines3[extra] torch gymnasium
gymnasium>=0.29.0           # Entorno RL (sucesor de OpenAI Gym)
stable-baselines3>=2.3.0    # PPO, DQN, A2C — algoritmos RL de producción
# torch>=2.0.0             # PyTorch — instalado via stable-baselines3[extra]

# ── Dashboard estático ────────────────────────────────────────────────────────
aiofiles>=23.0.0             # Requerido por FastAPI StaticFiles (sirve /ui)

# ── Fase 3: Retraining + Visual ───────────────────────────────────────────────
optuna>=3.6.0              # Hyperparameter search — retraining_scheduler
easyocr>=1.7.0             # OCR de precios desde charts — visual_state_builder
pyautogui>=0.9.54          # chart_launcher.py — abrir TradingView en Chrome (atajos teclado)
# opencv-python ya en stack (visual_state_builder, YOLOv8)
# ultralytics (YOLOv8) ya en stack

# ── Real-time data feed ───────────────────────────────────────────────────────
# websockets ya instalado en venv (listado arriba)
# Para Alpaca Market Data WebSocket (stocks US — gratis con API key):
# pip install alpaca-py
# alpaca-py>=0.20.0         # Alpaca SDK — opcional si se usa acceso directo WS

# ── Fase 3 — escáner (CNN-LSTM, Prophet, on-chain, fundamentales) ─────────────
# Nota: en PyPI el cliente ligero es "glassnode", no "glassnode-api".
tensorflow>=2.14.0
prophet>=1.1.0
yfinance>=0.2.40
glassnode>=0.0.2
