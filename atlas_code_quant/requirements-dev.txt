# Atlas Code-Quant — entorno de desarrollo (además del core)
# Instalación: pip install -r requirements.txt && pip install -r requirements-dev.txt
#
# Incluye herramientas que no son necesarias en producción mínima.
-r requirements.txt

# Runtime usado por tests de integración (operation_center, TZ mercado)
pytz>=2024.1

# Calidad / tests locales (opcional)
pytest>=8.0.0
# pytest-xdist>=3.5.0
# ruff>=0.4.0
