# Atlas Options Brain – Fase 1
requests>=2.31.0
python-dotenv>=1.0.0

# Proveedor gratuito (dev/backtest)
yfinance>=0.2.38

# Proveedor de pago recomendado (producción)
# pip install tradier  # sdk no oficial, usar requests directo (ya incluido)

# Análisis y backtest (Fase 2)
pandas>=2.2.0
numpy>=1.26.0
