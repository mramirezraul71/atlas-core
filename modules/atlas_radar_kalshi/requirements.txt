# Dependencias específicas del módulo atlas_radar_kalshi
# Instalación aislada:
#   pip install -r modules/atlas_radar_kalshi/requirements.txt

# --- Kalshi v2 ---------------------------------------------------------------
kalshi-python>=2.0.0          # SDK oficial (REST)
cryptography>=42.0.0          # firma RSA-PSS (llave .pem)
websockets>=12.0              # cliente WS para /trade-api/ws/v2

# --- HTTP / async ------------------------------------------------------------
httpx>=0.27.0                 # cliente REST asíncrono
anyio>=4.3.0

# --- Datos / cuant -----------------------------------------------------------
pandas>=2.2.0                 # series temporales de precios
numpy>=1.26.0                 # álgebra para Markov + Monte Carlo

# --- Tipado / validación -----------------------------------------------------
pydantic>=2.6.0               # DTOs y settings tipados

# --- Dashboard (FastAPI router montable en atlas-core :8791) ----------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0     # solo necesario en modo stand-alone (:8792)

# --- Polymarket CLOB (live; instalar para órdenes reales) -------------------
py-clob-client>=0.34.0

# --- Configuración -----------------------------------------------------------
python-dotenv>=1.0.1

# --- Logging / observabilidad ------------------------------------------------
# Reutilizamos logging stdlib + RotatingFileHandler; sin dependencia extra.

# --- Calibración probabilística (opcional, fallback a Platt si falta) -----
scikit-learn>=1.4.0           # IsotonicRegression para calibration.py

# --- Test (solo dev) ---------------------------------------------------------
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
