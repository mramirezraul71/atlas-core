### Umbrella review summary (PR #3)

This pull request is being used as the **umbrella review thread** for the audit remediation work on `variante/nueva` against `intent-input-rename`. Functionally, it bundles **paper safety** (market-hours gating, exposure caps, paper exit governance, and post-timeout order reconciliation) with the **second-wave paper-first runtime/risk** items already integrated on the branch: **conservative equity Kelly sizing**, a **minimal MLSignalRanker runtime layer** plus **scanner candidate context enrichment**, **`market_open_config.json`** as the operational source of truth for risk/schedule (with **explicit env > JSON > defaults** precedence), **canonical `atlas_core` documentation + manifest/verification scaffolding**, and **paper-first operational self-audit** wired through readiness as **read-only observability** (no new hard vetoes).

**Live trading is explicitly out of scope for this umbrella:** nothing here should be read as enabling production/live execution, hard **BLOCK** semantics for self-audit, or aggressive live sizing paths; those remain deferred to **separate, explicit PRs/phases** once readiness criteria and operator controls are agreed. The remaining review work should focus on confirming that, **taken together**, these changes satisfy the **paper/sandbox audit report** intent: bounded modules, expected test coverage (paper safety / Kelly / ML runtime / `market_open_config` / atlas_core manifest / operational self-audit), and no unintended coupling into execution or journal beyond what each patch claims.

Because the underlying branch diff is very large, reviewers should anchor on the PR description’s patch boundaries and spot-check the touched paths/tests rather than treating every unrelated historical file as in-scope for this audit sign-off.
