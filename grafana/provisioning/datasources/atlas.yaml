apiVersion: 1
datasources:
  - name: atlas-prometheus
    uid: atlas-prom
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
    editable: true
    jsonData:
      httpMethod: GET
      timeInterval: '2s'
